package dev.karatkevich

import io.kotest.assertions.fail
import io.kotest.core.spec.style.DescribeSpec

class GetArticlesRouteTest : DescribeSpec({
    describe("failing integration test") {
        it("should fail") {
            fail("failed")
        }
    }
})
